import React, { useEffect, useState } from 'react';

const index = () => {
	useEffect(() => {
		document.title = 'Todo';
	});

	return (
		<React.Fragment>
			<div>hello</div>
		</React.Fragment>
	);
};

export default index;
